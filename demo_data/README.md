# Synthetic demo artifacts

Files in this directory are generated by `scripts/generate_demo_data.py`.
They are deterministic synthetic fixtures used so a fresh clone renders a
complete dashboard without broker access.

They are not live or historical market observations, investment signals,
customer records, broker exports or performance claims.
