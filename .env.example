# Windows Store edition: read-only policy is compiled in and no environment
# value can enable external connections, financial accounts, or execution.
AEGIS_ENABLE_SIMULATION_EXECUTION=0

# AEGIS_DATA_ROOT and AEGIS_DATA_ROOT_BINDING are supplied together by the
# packaged WinUI shell. Do not set either value in a Store deployment.
