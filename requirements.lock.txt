# The Windows Store sidecar uses only the CPython standard library.
# Non-Store research dependencies live in requirements.txt and are excluded
# from the frozen sidecar, Store package and Store runtime legal inventory.
